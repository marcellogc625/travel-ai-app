<form method="post" action="{{ path('app_destino_delete', {'id': destino.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ destino.id) }}">
    <button class="btn">Delete</button>
</form>
